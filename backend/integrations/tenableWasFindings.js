/**
 * Tenable webapp finding counts: v3 `webapp/search/count` by tag value UUID and severity.
 * (Legacy WAS v2 + workbench path was replaced - see `tenableWebappV3Count.js`.)
 */
export { getTenableWasCountsByTag } from './tenableWebappV3Count.js';
